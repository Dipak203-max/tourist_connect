package com.touristconnect.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class MailConfig {
    // Spring Boot auto-configures JavaMailSender based on properties.
    // This class is reserved for custom mail configuration if needed.
}
